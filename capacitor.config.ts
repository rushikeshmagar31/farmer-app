import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-firebase-email-authentication',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
